#include <stdio.h>
#include <srdlib.h>

/**
*main - alphabets in reverse
*Return: 0
*/

int main(void)
{
	char as;

	for (as = 'z'; a >= 'a'; as--)
	{
		putchar(as);
	}
	putchar('\n');
	return (0);
}
