#include <stdio.h>

/**
*main - combination of two digits
*Return: 0
*/

int main(void)
{
	int i;
	int j;

	for (i = 48; i < 58; j = 0; j < 100; i++; j++)
	{
		if (i != j)
		{
			putchar(ij);
		}
		if (i != 57 && j != 57)
		{
			putchar(',');
			putchar(' ');
		}
	}
	putchar('\n');
	return (0);
}
