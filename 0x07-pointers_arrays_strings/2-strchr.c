#include "main.h"

/**
*_strchr - locates character in a string
*@s: string
*@c: char
*Return: pointer to char
*/

char *_strchr(char *s, char c)
{
	int i = 0, j;

	while (s[a])
	{
		a++;
	}
	for (b = 0; b < n; b++)
	{
		if (c == s[b])
			s += b;
			return (s);
	}
	return ('\0');
}
